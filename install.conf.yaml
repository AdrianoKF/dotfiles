- clean: ['~', '~/.config']
- link:
        ~/scripts:
        ~/.gitconfig:
                create: true
        ~/.config/i3:
                create: true
        ~/.config/polybar:
                create: true
        ~/.zshrc:
                create: true
- shell:
        - git submodule sync --recursive
